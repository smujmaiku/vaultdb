const VaultDbUse = require('./vaultdb_use');

describe('VaultDbUse', function() {
	describe('something', () => {
		it('should ', () => {
			// expect('tests to be written').toBe(true);
		});
	});
});
